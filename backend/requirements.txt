fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
pydantic-settings==2.3.4
yfinance==0.2.40
pandas==2.2.2
numpy==1.26.4
pyarrow==16.1.0
feedparser==6.0.11
requests==2.32.3
# Google Trends (unofficial) for the Risk-Appetite search-interest correlation.
# Free source — Google rate-limits datacenter IPs, so results are cached 6h.
pytrends==4.9.2
lxml==5.2.2
beautifulsoup4==4.12.3
# optional: Chrome-impersonating TLS for scrape-blocked networks (GEX discovery)
# curl_cffi==0.7.1
httpx==0.27.0
python-jose[cryptography]==3.3.0
# password hashing for email/password accounts. We call bcrypt directly —
# passlib 1.7.4 is broken against bcrypt 5.x (version probe + 72-byte handling),
# so it is intentionally not used.
bcrypt==5.0.0
SQLAlchemy==2.0.31
# Postgres driver — used in production when DATABASE_URL is set (Render).
# Local dev falls back to SQLite and doesn't import this.
psycopg2-binary==2.9.9
